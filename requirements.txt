# Core
dash>=2.14
dash-bootstrap-components>=1.5
plotly>=5.18
pandas>=2.1
numpy>=1.24
requests>=2.31
scikit-learn>=1.3

# Optional (enables p-values, F-statistics in analytics)
statsmodels>=0.14
scipy>=1.11

# Note: sqlite3 is included with Python stdlib — no extra install needed
# Note: The app fetches data from:
#   - Energi Data Service (electricity prices & production)
#   - Statistics Denmark StatBank API (CPI, GDP, industrial production)
#   - Open-Meteo Archive API (historical temperature)
# All APIs are free, no keys required.
